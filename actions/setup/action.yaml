name: Setup Flux Mirror CLI
description: A GitHub Action for installing the Flux Mirror CLI
author: Stefan Prodan
branding:
  color: blue
  icon: copy
inputs:
  version:
    description: Flux Mirror version e.g. v0.1.0 (defaults to latest stable release)
    default: "latest"
    required: false
  bindir:
    description: Alternative location for the CLI binary, defaults to path relative to $RUNNER_TOOL_CACHE.
    required: false
  verify-attestation:
    description: Verify the release attestation with gh attestation verify.
    default: "true"
    required: false
outputs:
  version:
    description: Flux Mirror version
    value: ${{ steps.cli.outputs.version }}
runs:
  using: composite
  steps:
    - name: Download the Flux Mirror CLI binary to the runner's cache dir
      id: cli
      shell: bash
      env:
        FLUX_MIRROR_VERSION: ${{ inputs.version }}
        FLUX_MIRROR_URL: "https://github.com/fluxcd/flux-mirror/releases/latest"
        BIN_DIR: ${{ inputs.bindir }}
        VERIFY_ATTESTATION: ${{ inputs.verify-attestation }}
        GH_TOKEN: ${{ github.token }}
      run: |
        if [[ -z "$FLUX_MIRROR_VERSION" ]] || [[ "$FLUX_MIRROR_VERSION" = "latest" ]]; then
          FLUX_MIRROR_VERSION=$(curl -fsSL -I -w '%{url_effective}' -o /dev/null ${FLUX_MIRROR_URL} | grep -o 'v[0-9].*')
        fi
        if [[ -z "$FLUX_MIRROR_VERSION" ]]; then
          echo "Unable to determine the latest Flux Mirror version"
          exit 1
        fi
        if [[ $FLUX_MIRROR_VERSION = v* ]]; then
          FLUX_MIRROR_VERSION="${FLUX_MIRROR_VERSION:1}"
        fi

        OS=$(echo "${RUNNER_OS}" | tr '[:upper:]' '[:lower:]')
        if [[ "$OS" == "macos" ]]; then
          OS="darwin"
        fi

        ARCH=$(echo "${RUNNER_ARCH}" | tr '[:upper:]' '[:lower:]')
        if [[ "$ARCH" == "x64" ]]; then
          ARCH="amd64"
        elif [[ "$ARCH" == "x86" ]]; then
          ARCH="386"
        fi

        FLUX_MIRROR_EXEC_FILE="flux-mirror"
        if [[ "$OS" == "windows" ]]; then
            FLUX_MIRROR_EXEC_FILE="${FLUX_MIRROR_EXEC_FILE}.exe"
        fi

        FLUX_MIRROR_TOOL_DIR=$BIN_DIR
        if [[ -z "$FLUX_MIRROR_TOOL_DIR" ]]; then
          FLUX_MIRROR_TOOL_DIR="${RUNNER_TOOL_CACHE}/flux-mirror/${FLUX_MIRROR_VERSION}/${OS}/${ARCH}"
        fi
        if [[ ! -x "$FLUX_MIRROR_TOOL_DIR/$FLUX_MIRROR_EXEC_FILE" ]]; then
          DL_DIR="$(mktemp -dt flux-mirror-XXXXXX)"
          trap 'rm -rf $DL_DIR' EXIT

          echo "Downloading flux-mirror ${FLUX_MIRROR_VERSION} for ${OS}/${ARCH}"
          FLUX_MIRROR_TARGET_FILE="flux-mirror_${FLUX_MIRROR_VERSION}_${OS}_${ARCH}.tar.gz"
          if [[ "$OS" == "windows" ]]; then
            FLUX_MIRROR_TARGET_FILE="flux-mirror_${FLUX_MIRROR_VERSION}_${OS}_${ARCH}.zip"
          fi

          MIRROR_CHECKSUMS_FILE="flux-mirror_${FLUX_MIRROR_VERSION}_checksums.txt"

          MIRROR_DOWNLOAD_URL="https://github.com/fluxcd/flux-mirror/releases/download/v${FLUX_MIRROR_VERSION}/"

          curl -fsSL -o "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" "$MIRROR_DOWNLOAD_URL/$FLUX_MIRROR_TARGET_FILE"
          curl -fsSL -o "$DL_DIR/$MIRROR_CHECKSUMS_FILE" "$MIRROR_DOWNLOAD_URL/$MIRROR_CHECKSUMS_FILE"

          echo "Verifying checksum"
          sum=""
          if command -v openssl > /dev/null; then
            sum=$(openssl sha256 "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" | awk '{print $2}')
          elif command -v sha256sum > /dev/null; then
            sum=$(sha256sum "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" | awk '{print $1}')
          fi

          if [[ -z "$sum" ]]; then
            echo "Neither openssl nor sha256sum found. Cannot calculate checksum."
            exit 1
          fi

          expected_sum=$(grep " $FLUX_MIRROR_TARGET_FILE\$" "$DL_DIR/$MIRROR_CHECKSUMS_FILE" | awk '{print $1}')
          if [ "$sum" != "$expected_sum" ]; then
            echo "SHA sum of ${FLUX_MIRROR_TARGET_FILE} does not match. Aborting."
            exit 1
          fi

          if [[ "$VERIFY_ATTESTATION" != "false" ]]; then
            echo "Verifying release attestation"
            gh attestation verify "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" -R fluxcd/flux-mirror
          fi

          echo "Installing flux-mirror to ${FLUX_MIRROR_TOOL_DIR}"
          mkdir -p "$FLUX_MIRROR_TOOL_DIR"

          if [[ "$OS" == "windows" ]]; then
            unzip "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" "$FLUX_MIRROR_EXEC_FILE" -d "$FLUX_MIRROR_TOOL_DIR"
          else
            tar xzf "$DL_DIR/$FLUX_MIRROR_TARGET_FILE" -C "$FLUX_MIRROR_TOOL_DIR" $FLUX_MIRROR_EXEC_FILE
          fi

          chmod +x "$FLUX_MIRROR_TOOL_DIR/$FLUX_MIRROR_EXEC_FILE"
        fi

        echo "Adding flux-mirror to path"
        echo "$FLUX_MIRROR_TOOL_DIR" >> "$GITHUB_PATH"
        echo "version=v${FLUX_MIRROR_VERSION}" >> "$GITHUB_OUTPUT"
